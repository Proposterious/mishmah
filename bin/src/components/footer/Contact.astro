---
import { Icon } from "astro-icon/components";

interface Props {
	icon?: string;
	alt: string;
}

const { icon = "logo", alt } = Astro.props;
---

<div class="flex flex-row space-x-2 rounded-full p-4">
    <Icon name={icon} class="w-6 h-6" />
    {alt}
</div>