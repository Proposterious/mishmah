---
import { Icon } from "astro-icon/components";

interface Props {
    alt: string
    icon?: string
    href?: string
}

const { alt, icon = "logo", href } = Astro.props;
---

<div class="flex flex-col justify-center">
    <Icon class=""
    name={icon} href={href} />
    {alt}
</div>